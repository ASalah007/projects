const DEGREE =1; // TODO
const findBtn = document.getElementById('find');
const insBtn  = document.getElementById('ins');
const delBtn  = document.getElementById('del');
const par = document.getElementById('par');


findBtn.addEventListener('click', ev => find(par.value));
insBtn.addEventListener('click', ev => ins(par.value));
delBtn.addEventListener('click', ev => del(par.value));

function ins(element) {

}

function del(element) {
    
}

function find(element) {

}